import sugar
import denim_ui
import unittest

suite "event to observable tests":
  discard
  # TODO: Fix event to observable and tests
  # test "Test 1":
  #   var testEmitter = emitter[int]()

  #   var emitterVal = 0
  #   var observableVal = 0

  #   testEmitter.add(
  #     proc(newVal: int): void =
  #       emitterVal = newVal
  #   )
  #   let obs = testEmitter.toObservable()
  #   discard obs.subscribe(
  #     proc(newVal: int): void =
  #       observableVal = newVal
  #   )

  #   check(emitterVal == 0)
  #   check(observableVal == 0)

  #   testEmitter.emit(123)
  #   check(emitterVal == 123)
  #   check(observableVal == 123)


  #   testEmitter.emit(321)
  #   check(emitterVal == 321)
  #   check(observableVal == 321)
